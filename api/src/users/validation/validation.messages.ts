
const INVALID_PASSWORD = `Password should have at least 1 upper case letter, 1 lower case letter, a number and a special character`;
const INVALID_PASSWORDS_NOT_EQUAL = `passwords needs to match exactly`

export const MESSAGES = {
    INVALID_PASSWORD,
    INVALID_PASSWORDS_NOT_EQUAL
}